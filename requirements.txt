# Core — every machine (all five VMs, and the no-GPU laptop).
#
# Pinned for cross-VM byte-identical corruption regeneration. Bumping
# numpy/opencv/pillow invalidates comparability across shards — if you must
# bump one, re-run scripts/verify_determinism.sh on two VMs and compare
# hashes before you keep the change.
#
# Role-specific deps are NOT here, because the 90G disk means no VM holds both
# the editor and a judge:
#     judge VM   ->  pip install -r requirements-judge.txt
#     editor VM  ->  pip install -r requirements-editor.txt
#     stage 0    ->  pip install -r requirements-coco.txt
# Each of those pulls this file in, so install one command, not two.

numpy==1.26.4
# headless: the GUI build needs libGL.so.1 -> apt -> root. We have no root.
#
# 4.11.0.86, not 4.10.0.84: vllm 0.11.0 requires opencv-python-headless>=4.11.0,
# and 4.10 made the judge VM impossible to install. Before bumping, the fixture
# was rendered under both versions with numpy and Pillow held identical: all
# five corruptions and the total hash came out byte-identical
# (5073799d8511586afc2dc504b330abea under each). So this bump does not
# re-baseline anything. Still an exact pin, not >=4.11.0 — a range lets two VMs
# resolve to different builds, which is exactly what determinism forbids.
opencv-python-headless==4.11.0.86
Pillow==10.4.0
pandas==2.2.2
pyarrow==17.0.0
scikit-learn==1.5.1
tqdm==4.66.5
PyYAML==6.0.2
matplotlib==3.9.2

# Transfer (all VMs)
huggingface_hub==0.34.4
hf_transfer==0.1.8
