# Pinned for cross-VM byte-identical corruption regeneration.
# Bumping numpy/opencv/pillow invalidates comparability across shards —
# if you must bump, re-run scripts/verify_determinism.sh on two VMs first.
numpy==1.26.4
opencv-python-headless==4.10.0.84   # headless: GUI build needs libGL.so.1 -> apt -> root. We have no root.
Pillow==10.4.0
pandas==2.2.2
pyarrow==17.0.0
scikit-learn==1.5.1
tqdm==4.66.5
PyYAML==6.0.2
pycocotools==2.0.8
matplotlib==3.9.2

# Stage 3 (judge VMs). Prebuilt wheels, no apt needed.
# vllm>=0.11.0 required for Qwen3-VL support.
# vllm==0.11.0
# torch==2.8.0

# Stage 1 (editor VM only)
# diffusers==0.35.1
# transformers==4.56.0
# accelerate==1.10.0
# sentencepiece==0.2.0
# protobuf==5.28.0

# Transfer (all VMs)
huggingface_hub==0.34.4
hf_transfer==0.1.8
