# Stage 0 only — the COCO instance-seg filter. Installs core too.
#
# Split out because only stage 0 imports pycocotools, and a failure there must
# not block the four VMs that never run it. (It shipped manylinux wheels as of
# 2.0.11 and installed clean on the VMs with no compiler — the older warning
# about needing gcc no longer applies.) If it does fail, nothing
# outside stage0_coco.py imports it, so the rest of the pipeline is unaffected.
# Only the VM that runs stage 0 needs this.

-r requirements.txt

pycocotools==2.0.8
